// Add animations (optional)
document.addEventListener('scroll', () => {
    const features = document.querySelector('.features');
    const gadgets = document.querySelector('.gadgets');
    features.style.opacity = 1;
    gadgets.style.opacity = 1;
  });